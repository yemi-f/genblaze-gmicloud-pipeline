# Service layer — thin shim between runtime and repo.
# For this sample the service layer is intentionally minimal:
# all pipeline orchestration happens directly in runtime/runs.py,
# which calls repo/pipelines.py functions.
